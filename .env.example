# ================================
# TaniJual Environment Configuration
# ================================

# IMPORTANT: Copy this file and rename it based on your environment:
# - For development: Copy to .env.development
# - For production: Copy to .env.production
# - For active use: Copy the appropriate one to .env

# ================================
# DEVELOPMENT CONFIGURATION
# ================================
# Use this for local development with PostgreSQL

# Local PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL="postgresql://postgres:password@localhost:5432/tanijual_dev"

# Environment mode
NODE_ENV="development"

# ================================
# PRODUCTION CONFIGURATION
# ================================
# Use this for production with Supabase

# Supabase Pooled Connection (for application queries)
# Get from: Supabase Dashboard -> Project Settings -> Database -> Connection String -> Transaction mode
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Supabase Direct Connection (for migrations and schema changes)
# Get from: Supabase Dashboard -> Project Settings -> Database -> Connection String -> Session mode
# DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Environment mode
# NODE_ENV="production"

# ================================
# HOW TO GET SUPABASE CREDENTIALS
# ================================
# 1. Go to https://supabase.com
# 2. Select your project
# 3. Go to Project Settings (gear icon)
# 4. Click on "Database" in the left sidebar
# 5. Scroll to "Connection string" section
# 6. Copy "Transaction" mode for DATABASE_URL (pooled connection)
# 7. Copy "Session" mode for DIRECT_URL (direct connection)
# 8. Replace [YOUR-PASSWORD] with your database password

# ================================
# QUICK SETUP INSTRUCTIONS
# ================================

# Development Setup:
# 1. Copy this file to .env.development
# 2. Update DATABASE_URL with your local PostgreSQL credentials
# 3. Run: npm run db:generate
# 4. Run: npm run db:migrate:dev
# 5. Run: npm run dev

# Production Setup:
# 1. Copy this file to .env.production
# 2. Update DATABASE_URL and DIRECT_URL with your Supabase credentials
# 3. Run: npm run db:push:prod
# 4. Run: npm run build

# For more details, see: DATABASE_SETUP.md
